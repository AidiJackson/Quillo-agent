# Quillo API Configuration
# Uses server-side proxy (BFF) to avoid exposing API keys in browser
VITE_QUILLO_API_BASE=/ui/api

# UI Token (dev-only scaffolding; production uses session auth)
VITE_UI_TOKEN=dev-ui-token-12345
